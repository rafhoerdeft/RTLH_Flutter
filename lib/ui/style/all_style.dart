import 'package:division/division.dart';
import '../../config/config.dart';

ParentStyle boxStyle = ParentStyle()
  ..borderRadius(all: 10)
  ..background.color(pmColor)
  ..padding(all: 15);

TxtStyle txtStyle = TxtStyle()
  ..fontSize(20)
  // ..fontFamily('Nunito')
  ..bold(true)
  ..textColor(lightColor)
  ..textAlign.center();
