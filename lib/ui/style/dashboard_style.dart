import 'package:division/division.dart';
import 'package:flutter/material.dart';

ParentStyle headerDashStyle = ParentStyle()
  ..width(double.infinity)
  // ..height(65)
  // ..margin(left: 30, right: 30, top: 230, bottom: 20)
  ..borderRadius(bottomLeft: 30)
  // ..background.color(Color(0xFF2f2546))
  // ..border(bottom: 10, color: Color(0xFFcc0000))
  ..padding(all: 20);
// ..elevation(3, opacity: 0.5);

ParentStyle gridDashStyle = ParentStyle()
  ..borderRadius(all: 10)
  ..background.color(Color(0xFF9f9bb4))
  ..padding(all: 20);
// ..elevation(3, opacity: 0.5);
